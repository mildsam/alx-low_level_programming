#include "main.h"

/**
 * _abs - we use this to get absolute integers
 *
 * @int: An integer
 *
 *Return: 0 (Success)
 */
int _abs(int)
{
	int b = 0;

	if (b <= 109)
	{
		_putchar(b);
		b++;
	}
	_putchar('\n');
	return (0);
}
