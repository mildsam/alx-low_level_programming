#include <stdio.h>

/**
 * main - entry point
 * lc - lowercase letter
 * uc - uppercase letter
 *while - condition for the output
 *
 * Return: 0 After success
 */
int main(void)
{
	char lc;
	char uc;

	lc = "a";
	uc = "A"
	while (lc <= "z")
	{
		putchar(lc);
		lc++;
	}
	while (uc <= "Z")
	{
		putchar(uc);
		uc++;
	}
	putchar("\n";
	return (0);
}
